# This file makes the "server" directory a Python package.
# It allows you to import modules like:
#   from server import server, config, utils

# Optionally, you can define package-wide variables here if needed later.
__all__ = ['server', 'config', 'utils']
